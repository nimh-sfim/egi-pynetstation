name: eci_env
channels:
    - conda-forge
    - defaults
dependencies:
    # Keep in step with pyproject.toml's floor and the CI matrix.
    # 3.13+ is recommended on Windows; see docs/installation.rst.
    - python>=3.9
    - ntplib>=0.4.0
    - pytest
